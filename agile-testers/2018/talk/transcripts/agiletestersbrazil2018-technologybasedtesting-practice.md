I'm going to talk about technology-based technical testing and what that implies, because fundamentally, when we're doing Agile, we talk a lot about acceptance criteria and how do we test within Agile and dada, blah blah. That's all important, but we have to go beyond the acceptance criteria, and one way to do that is through the notion of technical risk.

If you want to do technical risk, you have to understand technology, you have to understand the technical testing. I'm using the phrase technical testing at this point in time, to cover ... I'm not defining it exclusively as this, but to cover identify technology-based risks to drive testing. That's a thought process that can make us informed about the decisions that we're going to take to define the scope of our testing. Not just to increase the scope, but to restrict the scope. To focus in on specific parts of the application, and not do everything across every browser.

It helps us also understand what we need to understand. What we need to study to improve our technical knowledge. In order to do that, we need to understand technology, we need to be able to identify risks, we need to identify tools that can help us in that.

Over the course of this presentation, I'm going to try and convince you of that by looking at really simple web technology and how the combination of different simple technologies can increase the risk to us in our testing, that we need to have technical knowledge to understand and restrict what we're doing. 

This talk arose because I was sent an email or a LinkedIn message. It was a really simple question. It was basically I know HTML, CSS, HTTP and JavaScript, so what do I learn next? The notion was well how do I then move into automating? Because I know all this other stuff, so I should be able to know just automate and then just ace ahead of this.

Now part of the issue here is I myself, even though I've automated for years, I'm still learning HTML, CSS, HTTP, JavaScript. I'm finding new ways to use that functionality. I'm finding ways to automate applications from within the browser, using JavaScript from the console itself. There's so much to learn here.

Rather than tell someone what to so, I asked question like well, do you actually know it? Do you know how to write HTML? Do you know how to write CSS? Do you understand everything you read? Do you know how your application is specifically using these technologies? Do you know what HTML your application is using? Do you know what specific CSS they're using? Is it CSS directly or is it through a SAS tool? Is it SCSS? Which can generate CSS for you, because all of these different parts of our pipeline might increase or might change the risk., we might not have to test certain things as a result of it. Do we know what elements on the page have specific JavaScript events? Do we know whether they bubble up? Do we know where we can catch them? Do we understand this from a technical point of view, to know where the risks are? Because if not, if we don't know how it's applied and we don't know how to validate it, we don't really know whether there's cross-browser risks. We're testing blind. There's a lot of stuff to learn here.

Simple things in combination can have complex side effects. If you've got an application and it's going through a CDN or a web server cache or a local browser cache, because everything gets cached locally, do you understand how your application responds to that cache? When you make changes, is the correct JavaScript picked up? Is the correct CSS picked up? Do you have anything in place for that? Have you even identified that as a particular risk, given the technology profile you have?

JavaScript can be attached directly to HTML or it can be attached to HTML after the page has loaded, through hooks, and we can dynamically allocate those events. Do we understand when that happens? Is it when it's first loaded? Is it after it's rendered? Is it after it's rendered and it's made all the Ajax calls to then increase more data? Do we know when that happens? Because there might be different effects on the user. It might happen fast enough that the user doesn't notice, but if the user is an automated execution tool, it might have effects. We need to understand this.

If we use parts of the HTML technology that are complicated, like CSS animations, that might impact what we have to test. It might impact how we can automate if something animates slower, but doesn't receive events until it's animated, because that can change in different browsers, or it might change on different mobile devices. When things are loaded dynamic, all of this stuff can have an impact on our testing and the application. If we don't understand it, we're not testing the application from that point of view. 

If we don't understand the technology, we're not testing for the technical risks effectively because we haven't considered them. We might have technical risks like known technical risks about specific versions of technology, but we're not understanding the interplay that is unique to our environment and our application.

When we look at Agile, Agile stories, they're a very vague and nebulous term. They vary between different teams, they're very often completely business focused. Sometimes they're just conversation markers because people get real zealous about their Agile approaches, and very often they accept the criteria provides just a minimum goodness. It's not this story is now acceptable, it's this story is acceptable. Doesn't mean it's ready to go out, doesn't mean it's good, doesn't mean it works, just means it's acceptable based on what we have currently discussed. But that's why we have testers, that's why we have professional developers, that's why, because we're just putting in the minimum, we expect people to go beyond that, so we have to be able to go beyond that.

Now a lot of the time, the acceptance criteria does not cover the technical considerations. It doesn't often specify if the validation is in the JavaScript or whether it's HTML5, or whether it's completely on the server side or whether it's a combination of all three. Very often, that's an implementation decision that the team make, possibly during the discussion of their stories, possibly during the estimation, possibly afterwards. Maybe they changed their mind afterwards. It varies where all this technical consideration comes in. May not specify the libraries, specific version of the libraries or how we are going to update this and maintain it in the future. It might specify and accept what browser range, this will work on Edge and IE9 and whatever else. 

Very often, that is a decision that is made once. We are going to target this application at Edge, Chrome, Firefox, and these specific range of versions and these mobile devices, because those are the top 50% of headers that we see coming in on the server side, but we're not really saying what we have to test in those. We've just specified a requirement, must work on these, give that off to the development team, blardy blardy blah, job done from the business side. Then the development team have to go, "Right, how do we know what works on which ones? All right, let's just test on everything." Clearly we can't test on everything, so we run a subset of tests. What is that subset based on? Don't know. Very often it's nothing. 

Very often we don't have the technical risks in there. We don't know whether the JavaScript we're writing has any issues on the different browsers. If it's in HTML5, do we assess the risk that someone might edit the DOM and then bypass everything? If we're using HTML5 and JavaScript, so we assess whether there's a risk that the functionality might conflict because we're augmenting existing browser functionality? Just this interplay is really hard because it's rarely covered and it's rarely discussed in meetings that certainly I'm involved in.

I'm going to give an example which is just a dropdown menu on a site. If that was a story, then the user must be able to navigate, and the acceptance criteria is we can see a dropdown menu, you can use it, you can click on things and navigate. Blardy blah, go. Implement, der der der, easy. We use dropdown menus everywhere. Right now, if we look at just from a testing perspective, we've got dropdown menus, we go well where is this supposed to work? What if this is on a tablet or a mobile and you can't hover, you have to click? Are we coding it for that? How is this working? Does it have to be accessible? Do we need to be able to use key presses? Should tab order make a difference? What screen size does this work on? There's a whole bunch of questions in there.

Now a general risk that's not even associated with that is just well how do we know that this link that I just clicked on is the correct one for this text? You ask these questions at the story meeting, and people go, "Okay, tell you what, we'll give you a browser list, you test on those, and we'll give you a list of the text and the links, you can check whether that works." Easy. The problem is we didn't consider any of the technology, so is JavaScript used? Is it purely CSS? Is it HTML? What's supposed to happen if the browser doesn't support that version of JavaScript or the CSS can't be loaded in, or are we using libraries to help with this? Bootstrap might have some stuff for dropdown menus, are we using that? What version? Everything. Caching, how do we deploy this? So much stuff that we really have to consider if we're going to test to from a technical perspective.

Now the reason that the technologies are so important is because we view applications in different ways. I'm going to show you three different ways of viewing the application, but the different views change how we test it because they change how we think about it, and they require different domain knowledge. Some of them is purely business knowledge, some of it is knowledge about the functionality, some is requirement, some is the actual technology, HTML, CSS et cetera. There's different tools required because we need to be able to manipulate it and interact with it in different ways, not just the browser. Sometimes we need to access the HTTP and we need different tools for that.

A non-technical view, which might be a purely requirement view or a spec view, would be we've got an app, that's the Selenium Simplified Search Engine there, and we've got a requirements documents. We've got a functional spec, we've got non-functional requirements, those are what we understand about the app, then we use that app to check whether the things in the requirements have been met. But then we can drop down, go right, let's understand the browser from a technical point of view, and we know that the browser consumes HTML, and then that renders forms and there's fields in the forms, and then we can put limits and validation on those forms. We can see the source code and we can read it, and is there JavaScript in there? What version and what validation? Are there any plugins in the browsers? Do we care? Is that an impact? That's a lot of stuff there. We have to know which ones impact our testing and which ones we have to test.

Then the browser has to communicate with the back end application, which it does over HTTP, and if we understand that, we can test at that level. We can send an HTTP request without having to go through the GUI. We can check the server validation, because sometimes you can't check the server validation because the things you want to test are trapped by the browser validation, so you have to bypass the browser validation in order to test the server validation. We have to be able to do that if we're going to test at all the different levels in the application.

Now we do this to identify new things to test and to try and limit our testing. I have an acronym, MORIM. It's very important if you're testing something, to have your own acronym, because that's how you get famous. You can trademark this, and that's mine, MORIM. From MORIM, we are modelling the application from multiple viewpoints and multiple technical levels, you saw that with the different views, and there's more views [inaudible 00:12:07]. The more that you understand the technology, the more you will see different views into the application. 

We use tools to observe the application in action, so we can see when the DOM changes, we can see what HTTP traffic is coming across, we can see so many things happening, and we have to think about that, we have to reflect on that to model the application better, to identify more risks, to approach our testing with intent, to identify new interface points that we can test in the application. We use tools to interrogate the application, to interrogate in the DOM, to interrogate in the HTTP, to really look at it in detail after we've made the request. We can manipulate the application, so we can change the DOM, we can edit the DOM, we can resend the HTTP requests but amend them, we can manipulate the application in so many different ways, and that's what I think about when I'm doing my technical testing, and risk fits in there because we model the application in terms of its risk. We're looking at the technology, we're interrogating it to look for elements of risk. We approach it with intent after thinking about it, to target specific risk.

I'm going to show you screenshots from an application called The Pulper. I'm going to look at version one. You can find this application on GitHub, it's part of my testing app. You can go github.com/eviltester/testingapp, you'll be able to download this and you can use if you want. But I'm really interested just in the dropdown menu of The Pulper. The dropdown menu, just a simple dropdown menu, but why are dropdown menus risky for web apps? We see them everywhere, how could there possibly be any risk from this? But the issue is dropdown menus don't exist. There's no HTML element for a dropdown menu. On desktop they exists as native controls. You might find that you've got a library or a framework that is giving you the ability to do dropdown menus, but natively they don't exist on HTML, so we have to simulate it. That means we have to write code, and any time we write code, there's a risk that something goes wrong.

Now if it did exist, it might look something like this, a DDM, dropdown menu has a menu item, then under that menu item, there's another dropdown menu, and we might do this. Why then, if it did exist, would this be less risky? It'd be less risky because it would just be HTML. The simpler we that we can make our applications, the less risky they become. It's standard. Therefor we could check whether it meets the standard. We can have validation tools that compare an application against the standard. because if it meets the standard and a browser said we support standard X for dropdown menus, then it should just work. 

There should be less testing we have to do in terms of this functionality on this browser. We might care about does it still have the right links? Have we built it in a standard, complaint way? But we shouldn't have to test it in detail because the browser implements it, that means that the cross-browser testing should be reduced, because we don't test browser functionality. We don't test the date dropdowns on HTML input fields because that's the browser. We check how the date interacts with our validation, interacts with when we we send to out, interacts how our server processes it, but we don't test the dropdown component itself. Dropdown component is just a way of putting a value in a form that we send. 

Now if it was standard, tools would probably support it, so automated tools would supported. Automated tools might even come with abstractions that make it easy, so we could say new dropdown menu, dot select this value. We don't have to write any code or abstracts to do it. When it is just HTML, when it supported by our tools and standards, it becomes so much less risky. The risk then comes from we augment it and add JavaScript on, when we put more CSS on, when we decide to animate it, then we get more risky parts because we've changed something that is effectively standard.

But if it doesn't exist, we have to implement it, and how do you implement it? Is it CSS? Is it pure CSS? Is it JavaScript? Is it pure JavaScript? What HTML have we used underneath it? I'm going to suggest that as an exercise, after this talk, when you surf the web and you're looking at different websites and you see a dropdown menu, inspect that dropdown menu and see if you can get a feel for how it's implemented. Is it CSS? Is it pure CSS? Is it JavaScript? All those things, because then you'll start to spot things in the wild. You'll start to assess risk by comparing it with other systems. Because if you're only testing your application, you don't necessarily have a lot of oracles to compare it to, and this is an easy way of learning the technology in more detail.

Standard risks are it might not render at all. Why not? Well maybe the CSS doesn't commit, maybe we don't get the HTML, maybe it's dynamic loaded and there's a JavaScript error somewhere on the page. There might be lots of reasons why it doesn't render. Because it's rendering and it's a dropdown, we might have overlapping dropdowns. Maybe we click on one and something else appears. Maybe there's animation errors. I don't know because I don't know the technology. The links might not work, why not? I don't know, just it's a link. A link might not work. It might not be responsive. In what way? How specifically? I don't know because I don't know the technology. All of this stuff is I can identify risks, but I'm identifying risks blind because I don't really understand it. I don't know if it's loaded dynamically, I don't know if I should care about that, I don't know if there's timing issues.

Without knowing the technology used, I wouldn't know which of those risks to target. I might come up with them, people might go, "Yes, that sounds bad if that happens," it might never happen, it might not be feasible because of the technology and the approach we've chosen. We have to know the technology in order to figure that out.

If it was I want to test the links, I might use a link checker, but I don't really know whether that works for non-exposed sub menus. Does it only check things that are visible or does it check things in the page? Does it check for links that aren't supposed to be active yet, because that user doesn't have permissions? Maybe they shouldn't be in the page. Does it extract stuff that's from the HTML or what is it doing? We don't even understand the link checker properly. Can we test the rendering independently of the link checking? Maybe we just need to use a browser link checker tool, but then maybe it only checks the links that are visible. Again, we need to technology understanding in order to know whether our mitigation approach is good enough.

Generally then, we drop down to the mitigation approach of well we'll test this in all browsers or we'll test this on all platforms, mobile, IOS, Android, blardy blah. But then, what does all mean? Everything that we care about or just every browser that we've got installed? Should we just create a supported browser list and stick to that? When we've got a browser list, do we need to create a supported resolution list? Because maybe people have different monitor sizes. I've got a huge monitor here and a tiny monitor there, do I need to test on the different resolutions? Do we need to decide on specific devices? Who's coming up with this and why? Well it's the users that use it. Yeah, but does that make a difference to what we've built? If we have the most simple HTML page and it validates, why would I put that though every browser? Because it's going to work. There's no CSS. It's simple HTML, there's a couple of tags, it's going to work. Why would I test that on every device, because we've said that we cover this. No, from a technical risk perspective, that makes no sense. 

We have to also decide what we're going to test. What do we test? Do we test that on every page? Do we test every combination of dropdown, and do I have to click on every link after rendering or do I do that once? Do I have to do that on every rendering of every size? What makes the difference? We don't know because we're testing blind, so we reduce risks by understanding the technology.

Now very often, our dropdown menu is represented by a list. A non-ordered list with list tags, but that's just a list. That would be rendered in HTML's list, so what makes it a dropdown? If we don't know, it's magic. Magic makes it happen. All the browser and HTML pixies that are rendering, they make it a dropdown. They go, "Oh, it's a dropdown. I'll render it as a drop ..." That's not what happens. The browser and HTML pixies are CSS and possibly JavaScript, or a combination of both, but different tech gives us different risks. 

on version one of the application, there's a problem. When I have a small screen, I click on books, I can see that the menu that appears is actually underneath the other ones, and I can't click on the sub-menu. It's not possible. That's a big problem. But if we had a standard browser set, would we find that? Well we might if we resize the browser. We might hit it, but if we have more technical knowledge, we might be able to predict that risk. Past experience, if you've got web designers, they'll know that this kind of thing can happen, or testers who tested a [inaudible 00:21:29], they might know that this can happen. Or if we understood the technology, we knew that there's a Z index styling possible from CSS. That there's Z index order, Z order on the page. That might drive us to think well, we've got something that might be impacted by that, have we styled it to increase the Z order? 

It was a really simple fix on my app. I just added the Z index in the CSS and it worked. There was no functional fixes there because this is a pure CSS. Was that just a rendering problem that turned into a functional problem?

Now if I look at a JavaScript example, and I've just picked this example because I found it, and not because it's a bad example, I think it's a good example, and it's on javascriptarray.com. When the slide's released, you'll be able to go to the link if you want. This is pure JavaScript, so it's a completely different technology. The same functionality, same basic functionality, I hover over something, there's a dropdown menu, click on the dropdown menu to go to a link, same basic functionality, but it's different technology so different risks. Should I approach the testing the same? I think the answer is no. I think we need to understand it to then decide what risks we've got, because this is JavaScript. It might use different underlying HTML, it might render different, so there might be a risk that it's not as responsive as the other one, but I only know that because of the technology understanding that I've got on it.

Now Amazon, they have a dropdown menu, you'll have seen it if you're using amazon. They don't use ULs and lists, they use divs and spans, which is fine. It all works, it looks [inaudible 00:23:07], but what if the CSS doesn't load? Do we test for fallback if the CSS isn't in the CDN, or the CDN goes down, or we lose sound on the cache, or we've got the wrong CSS present, or there's no JavaScript? Is the application designed to handle that?

This is what Amazon looks like without CSS, so I would say that it's not designed to handle not having CSS, because it's just a blob of text because it's divs and spans. It just appears. The Pulper, or anything that's using just normal lists, is almost by design, going to work when there's no CSS because you're going to see a menu. Now it might not be exactly what you want, but it's going to work and it's going to look like a menu. 

Do we even know how to test for fallback? Do you know how to not have the CSS loaded? Could you change the browser settings to do that? Do you know that in the Chrome Dev tools, you can block URLs so the CSS won't load? Could you use proxies to do it? Could you use proxies with autoresponders or just block requests, or put it in breakpoints and then say, "This ones not coming through"? Could you skip it? Are there plugins that you could use? I know how to do it with the Chrome Dev tools and proxies, I don't know if there's any plugins, I don't know if there's any browser settings because I have two ways of doing it and that's what I rely on. If they stop working, then I might have to investigate other approaches, but at least I know some approaches for doing it. 

But this also highlights the notion that tools are fundamental to approaching technology-based testing, and our technology-based testing is limited to the extent that the tools support what we want to do. If I can't manipulate the traffic such that the CSS never loads, my tools don't support me in doing fallback testing for this. I really want to understand the tech in order to explore the risks and do it.

Then the question is well what technology do I need to learn? How much HTML? What versions of HTML? How much CSS? Do I need to understand SAS as well as CSS? How much JavaScript? What do I need to learn? Fortunately we have an easy answer for this, because we only really need to understand the technology we're using in our application. Over time, that will build up, so we focus in on our app. Browsers tools let us do inspect, so we can see exactly what elements, we can see the DOM, and if there's anything there we don't understand in the application we're testing, we go and investigate, and that's the easiest way to learn this. This is a real way to make your technical skill development sustainable because I you incrementally build it based on what you are actually testing.

Now The Pulper uses really basic HTML. If I look at that, what risks can I see? It looks pretty standard to me. There's very few tags in there. Now I need to understand nav, I need to know what nav is because that wasn't around when I really started learning HTML, so that might be new to me if I'm old at this. I can see the links in there, the links are all relative, so it might change depending on what page we're in. Would that be an impact? Well it's got the slash at the front, so it's probably okay because it's starting from the root. It's not specifying the domain, so I don't have to worry about that. What risks are in there? 

Do the links work? Is there any styling? Well there's a suggestion of styling because we've got class and possible the ID. The ID might be there for automating, but the class suggests that we've got a styling on there. Is it valid HTML? Well it looks like it, but how would I know? Well I would have to run it through a validation tool. What we've got there, with out ideal model, DDM, MI, if we've got something that's isomorphic to our ideal, something that has the same structure, where we can demonstrate that something works on something, if something is isomorphic to it, it more likely has the same benefits. That's why people like isomorphic functions. We test one, it's more likely to work, we have a problem with one, there's more likely to be a problem with the other.

Analogous in HTML, to that DDM setup which was our ideal, if we start looking through HTML standards, there's a menu in HTMl, with menu items. Maybe that's perfect for what we want to do. Well if I look it up, I can see that there is a menu tag and it's used for context menus, like right click menus. Well that's not what I want. I want a dropdown menu. Also I can see that it's not particularly well supported, so that's not particularly good.

Have a look at MDM, the developer, Mozilla developer network, I can see that there's not got a lot of good compatibility, but I've used external oracles, W3C, the mozilla.org, the W3Schools, I'm using external oracles to help me boost my technical information, and to make risk assessments as to whether technology is suitable for what we want to do.

Now I could probably put that in and style it. Maybe it won't render, maybe it will. Maybe some browsers will handle it, don't know. I don't want to take that risk, but I do want to find authoritative oracles that I can consult to help me make decisions. Can I Use is quite good, it shows you what you can use in different browsers. The validator that's on w3.org can validate the HTML and CSS for you. If you use something like Charles Proxy, you can send files off to that validator, and validate it within the proxy as you're testing. All these tool combinations are useful [inaudible 00:28:53].

An analogous HTML to that is this UL. The unordered list with the list time. That could render, that could be perfectly valid HTML. It's standard. We might just be able to style it to turn it into something. 

Now, here's one with divs. We've got the same hierarchical structure and we're using classes in order to add the semantics. It's analogous, but are there any risks with that? Well there are if you understand divs, because then you know that div doesn't quite model it by default when it's rendered, but this models that hierarchy by default when rendered, so we have to have styling in order for the divs to work.

Divs, by default, have a new line before each, but we can use styling to get rid of that, so we need to understand the CSS interplay if we use something like pure divs and spans, in order to know where there's any risks or fallback. Then we can do something completely unconventional, and the more that you browse the web, you will see things like this, where we've got a div then we've got a list item, because we know that list items are good for things in a list and a menu item is really a list item. We want things structured so we put them in divs, but this won't validate, so there's a risk that browsers won't display this properly. There's a risk that the browsers fix this, and then it renders completely different, in ways we don't expect. 

This is the validator for it. If I zoom in on that error message, the list item is not allowed as a child element of the div. If we chose that, we'd be creating something that is invalid and exposing us to more cross-browser issues. By knowing that, it means we could fix this by not doing this, or we have to test at a cross-browser level. Sometimes if you're taking in third party applications, you don't have a lot of choice. You have to just do lots of testing.

Now the problem here is it's a risk. You don't know if it has an impact. It might not render, it might render incorrectly, because browsers want to render HTML. If you give a browser bad HTML, it wants to give that to the user because it assumes that the server's made a mistake, that the person can't create a good website, but it's working on behalf of the user, so it wants to render it. It might fix the HTML, it might put tags in differently, it might default putting a UL tag in, but it might put a UL tag in the wrong place. It might put a UL tag around each list item and that's not what we want. It should put UL tags around the div, and just within the div. Where is it going to put it? It doesn't know, so it has to guess, it has to make it up, or it just renders without that. But it does mean that we have a risk that it might not work cross-browser, so we have to test it on different browsers.

Then when it comes to CSS, we have to understand CSS. We have to just know what it is in order to identify risks. CSS is generally thought of as the thing that gives you the styling, but it can also give you animation, it can also construct images. It can give you a lot of functionality for hiding things, hovering over things, things that look like functionality you'd have to code for, you can add in CSS. We have to understand it from that basis, but fortunately there are validators and we can look at the CSS. Where you can look at everything in the browser to see what there is. 

With CSS, generally, you might have rendering issues. Browsers, because CSS is a standard, people want to start using what's in the standard before it's actually available in the browser, so browsers implement specific browser versions of CSS styling. Then the standard comes along, and we've still got those specific things in our HTML because we don't know if people are still using older browsers and haven't got the standard, but if we coded purely to standards and take the risk, or just take the hit if someone's using an older version of the browser, because remember, most browsers now auto-update, and they auto-update in the background without people being aware, so most people are probably using relatively up to date browsers and you don't have to keep coding for the old browsers.

Now when it comes to this, we have to understand specific types of tools, so we need to know the browser Dev tools, and the browser Dev tools are getting better and better. You can do a lot of stuff that you used to only do in proxies, in the browser Dev tools, so by looking in those, we will see the functionality they've got, and it varies between browsers, so we have to understand the Dev tools on different browsers.

Now the benefit is that different functionality leads to different testing opportunities, because with proxies, Charles can validate the pages. I don't need an external validation tool, although I do use one, but if I'm using Charles, I can send pages off. If I'm using Fiddler, I can have auto-responders, so I can simulate 500 messages very easily within the proxy tool. If I'm using Zap or Burp suite, I can fuzz the application and do a lot of HTTp level testing quite quickly. The different functionalities in the tool lend themselves to thinking about testing differently, which is why we need to understand these tools in more detail, and keep exploring them because they keep adding new functionality in there.

Now we get complexity arising from the combination of things. Here I've got some HTML and I've got some CSS, and it can render a basic menu, but if I add JavaScript in, I've got three things in interplay, and that combination is what leads to complexity. If I start styling it, maybe it needs certain IDs in there, in the HTML, if they're not there then the styling won't work. Maybe JavaScript hooks on in different ways. Maybe the ordering is important. The complexity and arise, we get emergent behaviour, emergent bugs just by the combination of things. Even things as simple as HTML, CSS and JavaScript.

We need to become technically aware. To use the technical knowledge to identify new risks. To use those risks to go beyond the acceptance criteria. Use our technical knowledge to go beyond the acceptance criteria. As soon as wee see validation, as soon as wee see validation is done in the client side and the server, we have to test both. The acceptance criteria might just say it validates.

We need to use the technical knowledge to limit our test scope. We can't just keep doing cross-browser testing blind, and automate all the things across all the browsers just in case. We need to limit the scope to be more effective. When we do that, we then need to identify the appropriate tools for the technology we're testing, for the testing we want to do. As we learn about the tools, that will drive and increase the amount of testing or types of testing that we can do. We also need to model the application from different technical perspectives. That is key to understanding this, and that ability grows over time, the more technically aware we become. 

In the slides, you'll find there's extra links for where we're going, but we're basically covered. Now what I want you to do is take away some exercises so that when you're using an app, you start looking at the source, you start investigating the DOM, you start understanding what is in there and researching is in there. You review the application that you're actually testing so you know whether you understand the fundamentals, because you at least need to understand those. 

Use other sites as potential oracles or potential different ways of doing things to see whether perhaps that approach is less risky than what you're doing, but keep identifying risks. On every site you go to, identify the risks. Why did I pick those examples, the Amazon one? Why did I do that? Because when I looked at the source, I identified a risk that when the CSS isn't there, it won't render well, and it didn't render as well as something that uses a dropdown list. 

Thanks for watching. You can find me at eviltester.com and follow me on Twitter, but do keep improving your technical skills because that is key to getting better at all of this, and have a look at the slides when you've finished because there's more links and stuff okay? Thanks very much.